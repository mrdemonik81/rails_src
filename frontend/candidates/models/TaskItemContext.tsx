type TaskItemContext = {
  question?: string;
  minimumWords?: number;
  minumumDuration?: number;
  veracity?: boolean;
  statement?: string;
  arrangeWords?: string;
  additionalWords?: string;
};

export default TaskItemContext;
