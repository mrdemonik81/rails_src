# frozen_string_literal: true

When(/^I click assign exam to candidate and enter the following data:$/) do |_table|
  # table is a table.hashes.keys # => [:candidate_id, :client_id, :exam_id, :expires_at]
  pending
end
